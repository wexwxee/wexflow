# 🔄 ПЕРЕДАЧА: аудит и починка WexFlow

> Актуально на: `HEAD 0e79f46`, ветка `redesign`, версия **1.1.0** (готово к выкату).
> Обновлять этот файл в конце каждой сессии.

## Контекст
**WexFlow** (`C:\saling`) — десктоп-приложение (FastAPI + Jinja + Playwright + PyInstaller),
которое под именем реального человека подаёт заявки на вакансии Salling/7-Eleven. Есть автопилот,
облачная синхронизация, Telegram Mini App, аккаунт/тарифы. **Пользователь — не программист.**
Цена ошибки высокая: лишняя/ошибочная подача необратима. Идёт **поэтапная починка** по аудиту из
44 находок (F1–F44).

## 🚨 ЖЁСТКИЕ ПРАВИЛА
1. Правки **только в ветке `redesign`**. `master` неприкосновенен.
2. **Перед каждой правкой — бэкап** меняемых файлов в `.bak_<что>_<дата>/` (в `.gitignore`).
3. **НИКОГДА не запускать реальную отправку** (Indsend/Submit/«Запустить подачу»/«Автоотправка»),
   даже для проверки. Только чтение/рендер/тесты.
4. **Не писать в реальные данные** (`profile.json`, `jobs.db`, `secrets.json`, `settings.json`,
   `%AppData%\WexFlow\*`). Дев-данные в `C:\saling` — тоже избегать записи.
5. Каждый шаг = **маленький коммит** (рус.), в конце строкой
   `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
6. Объяснять простым языком; в конце шага — короткий отчёт.
7. **`app.py` делит Codex** — трогать минимально, `git status` перед правкой, коммитить сразу.

## 🧪 Как проверять без реальной подачи
- **Автономный рендер шаблона** (без импорта `app`): `jinja2.Environment(loader=FileSystemLoader('templates'))`,
  в `env.globals` стабы (`brand_label`, `L=labels`, `plan_label`, `show_billing=lambda:False`,
  `CHANGELOG=[]`, `APP_VERSION=""`) + стаб `request` с `.query_params.get`. `sys.path.insert(0, os.getcwd())`.
- **Только синтаксис шаблона**: `jinja2.Environment().parse(open(f).read())`.
- **Тесты**: `PYTHONUTF8=1 .venv/Scripts/python.exe tests/<файл>.py` (pytest не установлен — самозапускаемые).
  8 зелёных: `test_is_leadership`, `test_submit_invariants`, `test_apply_serializer`, `test_settings_store`,
  `test_apply_race_guard`, `test_parsers`, `test_profile_store`, `test_audit`.
- **app.py**: `python -m py_compile app.py` + прогон тестов.
- Пиксельные скриншоты на этой оболочке **зависают** — не использовать.

## ✅ Что закрыто по аудиту (~30 из 44)
14 коммитов последней сессии (`69545d7` → `07398f7`): F18/F20/F21 микротексты · F19 единый словарь подачи ·
F3 «Автопилот» в оболочку · F9 мелкие шрифты→11px · F32 живое имя CV · F43 тесты парсеров ·
F38 лог сбоя запуска подачи · F37 устойчивость `profile.json` · F6 токен `--radius-card` ·
F10 токен `--on-accent` · бейдж «руководящая» · журнал аудита `/audit`.

## ✅ Сделано в эту сессию (2026-07-01)
- **F28** ✅ авто-обновление: `install_update` только `https`; скачанный zip сверяется с
  SHA-256 из доверенного канала (GitHub API: поле `digest`, запасной путь — `*.sha256`),
  при несовпадении/отсутствии суммы — НЕ ставим, уводим на страницу загрузки. `publish_release.py`
  кладёт `WexFlow-<ver>.zip.sha256`. Тест `test_update_verify.py`. Коммит `1225a4f`.
- **F27** ✅ подача по облачному решению: `autopilot.tg_submit_batch` теперь подаёт только
  вакансии из журнала предложенного (`tg_offered_ids`) — чистый `partition_offered`, причина
  отказа `not_offered`. Тест `test_tg_offered_gate.py`. Коммит `484d87f`.
- **F41** ✅ единый страж локальных запросов: барьер loopback+CSRF был скопирован в `app.py`/
  `hub.py`/`connectors/webapp.py` → свёл в `local_guard.py`, в файлах тонкие адаптеры (−66 строк
  дублей). Поведение прежнее (характеризационный `test_local_guard.py`). Коммит `019b532`.
- **F36** ✅ перф: на главной `autopilot_count` показывается лишь при включённом автопилоте, а
  `match_count()`=`len(find_matches())` — полный проход по базе. Теперь считаем только при `enabled`
  (заодно убран дубль `get_rule()`). Поведение прежнее. Коммит `da6b04b`.
- **F16** ✅ страница «Справка» (FAQ) простыми словами + пункт в меню; `GET /help` → `help.html`. `50bb571`.
- **F2** ✅ единый вход через «Аккаунт»: в карте настроек Telegram-карточка прямо шлёт подключаться в
  «Аккаунт» (логин и так был только там; `/settings/telegram` — только операции). `d139e8b`.
- **F1** ✅ убрал дубли навигации из шапок (`← На главную`/`К вакансиям` = пункты бокового меню) в
  settings/overview/help/audit. Контекстные «назад» и переключатель Salling оставлены. `9b66e07`.

## ⏳ Что осталось (~7)
**Безопасность/доверие:**
- **F35** 🟠 окно «отправлено-но-не-записано» (нужна песочница Salling).
- **F29** 🟠 доверие облаку только по `device_id`, без подписи — по-настоящему чинится СЕРВЕРНО
  (подпись/ключ на аккаунт). Локально можно лишь смягчить: не слать `device_id` в адресе GET.
- **F30** 🟡 локальный тариф не должен быть границей безопасности (серверный гейт; `ENFORCE=False`
  сейчас — не эксплуатируется, вопрос будущего при включении оплаты).
- **F28 Уровень 2** 🟡 подпись архива обновления секретным ключом (защита даже от взлома репо);
  требует, чтобы Иван берёг приватный ключ. Пока сделан Уровень 1 (контрольная сумма).

**UX:**
- **F5** 🟡 «много акцентных цветов» — ОЦЕНЕНО: палитра уже дисциплинирована (один акцент teal + штатные
  ok/err/warn/info, токены, AA). Не дефект — вкусовое. Рекомендация: оставить; при желании позже
  подогнать цвета кнопок окна (жёлт/тиал/розовый) под акцент — но это по глазам Ивана на собранном app.
- **F4** 🟡 два входа в «Аккаунт» — оценено как не-баг.
- (F1, F2, F16 — сделаны, см. выше.)

**Тех-долг:**
- **F42** магические лимиты (низкая польза).
- **F44** засор рабочей папки — ОЦЕНЕНО: весь мусор уже в `.gitignore` (репо/сборка чистые,
  Codex не мешает). Осталась лишь локальная уборка диска (`dist/` ~7.5 ГБ регенерируется;
  `WexFlow_OLD_DO_NOT_USE.exe`, `WexFlow_dist.spec.bak` — явный мусор) — по решению Ивана, не код.
  `.bak_*` (бэкапы отката) не трогать.

## 📌 Технические заметки
- **Журнал аудита**: `GET /audit` → `audit.html`; хелперы `_audit_day_label`/`_audit_groups` (`test_audit.py`);
  запрос только `Job.applied_at is not null`, read-only.
- **Руководящие**: бейдж через глобаль `L.is_leadership(title)` в `index.html`/`detail.html` (без правок `app.py`).
- **Токены темы**: `--radius-card`, `--on-accent`. Правило «на акценте — токен `--on-accent`».
- **`profile_store`**: `save_profile` атомарный (tmp+os.replace) + зеркало `.bak`; `load_profile` терпит битый JSON.
- **Инварианты**: `applied_at` = нерушимая правда; узкое место подачи — `app._run_apply_worker`;
  отсев — `app._partition_submit_ids`; руководящие — `labels.is_leadership`.
- **Карта**: `app.py`(роуты), `apply.py`(Playwright-воркер), `autopilot.py`, `scraper.py`, `db.py`,
  `settings_store.py`/`profile_store.py`/`credentials_store.py`/`account.py`/`cloud_auth.py`.
  Шаблоны: `index.html`, `detail.html`, `apply.html`, `account.html`, `settings.html`,
  `autopilot.html`, `audit.html`, `_ui.html`, `_icons.html`, `base.html`. Стили: `static/theme.css`.
  Версия: `version.py`; журнал: `changelog.py`.

## 🚀 Релиз (готово к выкату 1.1.0)
Версия поднята до **1.1.0** (`version.py`), добавлена запись в `changelog.py` (совпадает с версией).
Всё локально-доделываемое закрыто; блокеров нет. Выкат — на машине Ивана:
1. `СОБРАТЬ_УСТАНОВЩИК.bat` (обновить `WexFlow-Setup.exe` — иначе `publish_release` не пустит).
2. `ВЫПУСТИТЬ_ОБНОВУ.bat` = PyInstaller → `package_dist.py` (zip) → `publish_release.py` (спросит y/n,
   приложит `.zip` + `.sha256` + Setup.exe). Собирает из текущей ветки (`redesign`), тег на её HEAD —
   **`master` не трогается**.
3. ПЕРЕД публикацией: собрать и запустить `.exe`, прокликать ключевые экраны — **без реальной подачи**.
   Рекомендация: сначала убедиться, что собранный app работает, и только потом жать публикацию (она =
   выкат всем: у кого версия ниже — прилетит авто-обновление).

## 🎯 Что осталось ПОСЛЕ релиза (не блокеры)
- **F29/F30** — серверно (Vercel-бот `wexflow-bot`), вместе с облачной платформой (память `project-cloud-platform`).
- **F35** — нужна песочница Salling.
- **F28 Уровень 2** — подпись обновления ключом (нужен приватный ключ Ивана); Уровень 1 (сумма) уже в 1.1.0.
- **F5** — вкусовое (палитра уже дисциплинирована); **F42** — низкая польза; **F44** — локальная уборка диска.
