ГДЕ ЧТО ЛЕЖИТ (памятка, обновлена 11.06.2026)
================================================

ПРОГРАММА СОСТОИТ ИЗ ДВУХ ПАПОК-ПОЛОВИНОК (так была устроена с самого начала):

  C:\saling          — ГЛАВНАЯ папка. Здесь: запуск всей программы, часть
                       для Salling (Netto/Føtex/Bilka), общая страница-переключатель.
  C:\seven11-apply   — вторая половинка: часть для 7-Eleven.

Запускается ВСЁ из главной папки: двойной клик по C:\saling\JobApplyHub.bat
(он сам поднимает и Salling, и 7-Eleven, и открывает браузер).

КНОПКИ ОТКАТА (лежат в главной папке C:\saling):
  ВЕРНУТЬ_СТАРЫЙ_ДИЗАЙН.bat   — вернуть старую проверенную версию (обеих половинок)
  ВКЛЮЧИТЬ_НОВЫЙ_ДИЗАЙН.bat   — включить новый дизайн обратно
  После нажатия — перезапустить программу через JobApplyHub.bat.
  Эти кнопки переключают ОБЕ папки сразу, ничего не удаляют.

БЭКАПЫ:
  C:\saling\backup\7eleven_dizajn_do_redizajna\   — копия старого вида 7-Eleven (для удобства, в главной папке)
  C:\seven11-apply\backup\2026-06-11_do_redizajna\ — то же самое, оригинал
  C:\seven11-apply\backups\working-20260528-130706\ — старая рабочая копия 7-Eleven от 28.05 (делалась раньше, не мной; не трогать)
  Плюс главная защита: вся история изменений хранится в git в обеих папках —
  любой шаг можно вернуть.

КОПИЯ ПРОЕКТА НА ДИСКЕ ОДНА. Других копий нет (проверено поиском по C:\,
Рабочему столу, Документам и Загрузкам).

ЛИЧНЫЕ ДАННЫЕ (пароли, профиль, CV) лежат только в этих двух папках,
в git не попадают и никуда не отправляются.
